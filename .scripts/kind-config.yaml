# 3-node local Kubernetes cluster: 1 control-plane + 2 workers
kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
name: delivery-booking
nodes:
  - role: control-plane
  - role: worker
  - role: worker
